<div{{ attributes }}>
    {{ form_start(form) }}
    <div class="quotation-info">
        {{ form_row(form.quotationId, {
                    label: 'Référence du devis',
                    value: 'something'
                })
        }}
    </div>
    <div class="quotation-line-container">
        <table class="quotation-line">
            <thead>
                <tr>
                    <th>Désignation</th>
                    <th>Description</th>
                    <th>Quantité</th>
                    <th>Prix</th>
                    <th>TVA</th>
                    <th>Remise</th>
                    <th>Total HT</th>
                    <th>Total TTC</th>
                </tr>
            </thead>
            <tbody>
                {% for item_form in form.quotationLines %}
                   <twig:Quotations:QuotationLineComp :item_form="item_form" :quotation="quotation" />
                    
                {% endfor %}
            </tbody>
            <tfoot>
                <tr>
                    <td colspan="6">Total </td>
                    <td>prix HT</td>
                    <td>prix TTC</td>
                </tr>
            </tfoot>
        </table>
         {{ form_widget(form.quotationLines.vars.button_add,{
        label: 'Ajouter un item',
        attr: {class: 'btn btn-primary'}
    }) }}
    </div>
    <button class="btn btn-success" type="submit" formnovalidate>Enregistrer</button>
        {{ form_end(form) }}
</div>
